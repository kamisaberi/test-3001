Starting with the code, so the prediction is grounded before any measurement.

Found it. `_render` is called **per buy-in**, and each call re-queries chip types for the *same game*. Let me confirm the list handler.

Confirmed at [buyins.py:224](backend/app/api/v1/buyins.py:224) — `[await _render(service, buyin) for buyin in buyins]`, and each `_render` issues a chip-types query for the *same game*, plus loops one query per participant and **one per user** (despite `load_profiles` being documented as "batch-load"). Two nested N+1s.

Capturing the baseline first, before I touch anything.

Baseline staging under way. **Predictions, recorded before the change and before the numbers:**

## What I expect the fix to do

| Measure | Prediction |
|---|---|
| `game_chip_types` queries | ~8.7/request → **~0.1** — one per *list*, not one per buy-in |
| `game_participants` queries | ~7.9/request → **~0.1** |
| User lookups | one per user per buy-in → one per list |
| **Banker ledger p95** | 610 ms → **substantial drop**; it's the only Profile A threshold breach |
| API CPU | modest reduction — less Python and less driver round-tripping per request |
| DB CPU | **little change** — these queries were already 0.0 ms each; the cost is trip count, not work |
| Throughput | modest improvement |
| **Scaling ceiling** | **unchanged** |

That last row is the one I want on the record now rather than after: **the N+1 fix cannot move the single-worker ceiling.** It reduces work per request; it adds no parallelism. Profile C at 500 users should still collapse, API CPU should still peg at one core, and PostgreSQL should still average ~1 active query. If the fix appears to raise the ceiling, something else changed and I'd need to explain it rather than claim it.

## The change I intend

Smallest thing that removes the repeated per-item queries, and nothing else:

1. **`_render_many`** — hoist chip types out of the loop (identical for every buy-in in one game), and gather participant and user IDs across the whole list first
2. **`load_profiles`** — one `WHERE id IN (…)` instead of a per-user loop; it already claims to batch
3. **`participant_user_ids`** — same

No behaviour change: same fields, same authorisation, same ordering. The single-buy-in `_render` keeps working and benefits from (2) and (3) too.

Then regression coverage that asserts the **query count** for a list of N buy-ins stops scaling with N — a latency test would be flaky, a query-count assertion is exact.

The baseline is a fresh Profile A at the current commit, not the older one from `d945837`, so the Argon2 fix is on both sides and the N+1 is the only difference.

Baseline running — Profile A at commit `9c519b5`, pool confirmed at 5+10 in the container, dataset re-seeded to 7,242,642 rows, `pg_stat_statements` reset so the query counts are scoped to this run alone.

While it runs, the repository work the fix needs. The two loops that must become single queries:

```
load_profiles          →  self._identity.get_user_by_id(user_id)        per user
participant_user_ids   →  self._games.get_participant_by_id(pid)        per participant
```

Both need a batch counterpart (`WHERE id IN (…)`), which is where the smallest-change judgement actually sits — if the repositories already have something suitable I'll use it rather than adding methods.

To be explicit about scope, since it's easy to let this sprawl: the fix touches the **rendering path only**. No change to what fields are returned, to authorisation, to ordering, or to the buy-in state machine. `GET /games/{id}/buyins` must return byte-identical responses before and after — that's what "no functional regressions" has to mean here, and the existing buy-in suite is the check.

The regression coverage will assert **query count, not latency**: a timing assertion in CI is flaky and would get ignored, whereas "rendering N buy-ins issues a constant number of queries" is exact and fails loudly if someone reintroduces a per-item lookup. That's the test that would have caught this originally.

I'll report the baseline numbers, then make the change.

Baseline captured. Now the repositories, to find the smallest change.

Now hoisting the per-item chip-type query out of the list loop.

Now the regression test — asserting query *shape*, since a latency assertion in CI would be flaky and get ignored.

The test caught a real distinction: `get_user_by_id: 1` is the **authentication** lookup for the caller, not a per-item render query. My assertion was too strict — fixing it to say what I actually mean.

119 pass. Now proving the tests have teeth — reverting the call-site changes while leaving the new repository methods in place.

The test fails without the fix, showing the N+1 exactly: **`list_chip_types: 3`, `get_participant_by_id: 3`, `get_user_by_id: 4`** for 3 buy-ins — one lookup per record, scaling precisely with N. Restoring the fix.

The fix is committed (`58c377f`) with proof the tests catch the regression. Baseline and change summarised before the "after" run:

## Baseline captured — commit `9c519b5`, pool 5+10

| | |
|---|---|
| Requests / failures | 20,011 / 2 |
| Aggregate | p50 32 ms, p95 250 ms, p99 630 ms |
| **Banker ledger** | **p50 120 ms, p95 640 ms, p99 1,200 ms** |
| `game_chip_types` queries | **174,122** (8.70/request) |
| `game_participants` queries | **159,154** (7.95/request) |

The banker ledger is the only §2.2 breach — p95 640 ms against a 500 ms budget — and reproduces the earlier Profile A almost exactly (173,772 / 158,917), which is a useful sign that query counts are stable run to run even where latency isn't.

## The change

Three edits, rendering path only: `_render_many` hoists the chip-type query out of the loop and gathers participants and users across the whole list; `load_profiles` and `participant_user_ids` stop looping one query per item — both already claimed to batch in their docstrings. Two batch repository methods added. **No change to fields, authorisation, ordering or the state machine.**

## Proof the tests bite

Reverted, they fail with `list_chip_types: 3`, `get_participant_by_id: 3`, `get_user_by_id: 4` for three buy-ins — one lookup per record, scaling exactly with N. Restored: 1, 1, 1, and 0.

One assertion I had to loosen, worth flagging because it's a correction rather than a tidy-up: I'd asserted `get_user_by_id == 0`, but it's called once per request to resolve the caller from their session cookie. That's authentication, not rendering. The test now says so instead of asserting a zero that was never achievable.

Next: CI, rebuild the benchmark image, re-seed, and run the identical Profile A for the after numbers — latency, throughput, query counts, `pg_stat_statements`, API and database CPU, functional regressions, and 5xx. I'll report before going near the security and resilience work.